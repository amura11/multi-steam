<Window x:Class="PlayniteMultiAccountSteamLibrary.TestHarness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlayniteMultiAccountSteamLibrary.TestHarness"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="5">
            <Button Content="Fetch Games" Click="OnFetchGameClicked"/>
            <!-- Add more buttons for testing commands or state -->
        </StackPanel>

        <ContentControl x:Name="ContentHost" Grid.Column="1" Margin="5"/>
    </Grid>
</Window>
